# Python
__pycache__/
*.pyc
.venv/
venv/

# Backend runtime database (regenerated by seeding — never commit real state)
backend/nexus.db

# Node (only used by the test harness, not by the shipped frontend)
frontend/node_modules/
frontend/package-lock.json

# Env
.env

# OS / editor
.DS_Store
*.swp

# Test artifacts
.pytest_cache/
